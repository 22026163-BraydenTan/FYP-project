name: test echo setup

on:
  push:
    branches: [ "features/action-workflow", "main" ]
    
jobs:
  job:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@4
    - uses: ./.github/workflow/template/echo.yaml
