name: Test Echo Setup
on:
  push:
    branches: [ "hotfix/echo-pipeline", "main" ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run Echo Action
        uses: ./.github/template/echo/action.yml # Reference to the directory for echo
      # - name: Run SonarCloud
      #   uses: ./.github/template/sonarcloud/action.yml # Reference to sonarcloud directory, use crt+ / to uncomment
